version: '3'

services:
  newman:
    build:
      context: ./
      dockerfile: Dockerfile-newman
    image: sweetcount/newman:latest
    volumes:
      - ./tests:/app
    entrypoint: ["/app/run_tests.sh"]
    networks:
      - backend